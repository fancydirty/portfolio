// No-op stub for the `server-only` package under the test runner, which has no
// react-server condition (the real module throws there). Owned by the repo so
// resolution does not depend on how the package is hoisted in node_modules.
export {};
